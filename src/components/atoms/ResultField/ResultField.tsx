// Function to create a Result in the table for the search

import React from "react";
import type { ResultFieldProps } from "./ResultFieldProps";

export const ResultField = (props: ResultFieldProps) => {
  return <div>Result Field Component</div>;
};
